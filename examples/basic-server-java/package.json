{
  "name": "@modelcontextprotocol/server-basic-java",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal MCP App server in Java",
  "scripts": {
    "build": "mvn -B package -DskipTests -q",
    "start": "mvn -B package -DskipTests -q && java -jar target/basic-server-java-1.0.0.jar",
    "dev": "mvn -B package -DskipTests -q && java -jar target/basic-server-java-1.0.0.jar"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.0"
  }
}
